# to-check/

Drop your work here — photos (e.g. `.jpg`, `.png`) or text files (e.g. `.txt`, `.md`, `.docx`) — and ask me to check it.

I'll look it over and write feedback in `../feedback/` with the same filename, explaining mistakes step by step with hints (not the final answers).
