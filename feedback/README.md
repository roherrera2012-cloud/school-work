# feedback/

This is where I write feedback on the work you upload to `../to-check/`.

Each file here matches a file in `to-check/` (same name, `.md` extension). Feedback explains mistakes one at a time with hints — never the final answer — so you can fix things yourself.
